<Window x:Class="ScritchyScratchyCheater.Views.Dialogs.MessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScritchyScratchyCheater.Views.Dialogs"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="CenterOwner"
        Title="Message" Height="190" Width="310"
        Background="{StaticResource Window.Background}">

    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="0"
                      CaptionHeight="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1" CornerRadius="0" Background="{StaticResource Window.Background}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical" Grid.Row="0" VerticalAlignment="Center" Margin="8,0,8,8">
                    <TextBlock x:Name="MessageTitle" Text="Title"
                               Style="{StaticResource TextBlock.Base}"
                               Margin="0,0,0,16" FontFamily="{StaticResource Montserrat}" FontWeight="Bold"/>
                    <TextBlock x:Name="MessageBody" Text="Message could not be displayed."
                               Style="{StaticResource TextBlock.Base}"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <Grid Grid.Row="1" Background="{StaticResource ControlElement.Flat.Background}">
                    <Grid.Resources>
                        <Style TargetType="Button" x:Key="btn" BasedOn="{StaticResource Button.Dialog}">
                            <Setter Property="Height" Value="26"/>
                            <Setter Property="Width" Value="60"/>
                        </Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </Grid.Resources>

                    <StackPanel Margin="8" x:Name="ButtonOk" Visibility="Visible">
                        <Button Style="{StaticResource btn}" Content="OK" Click="ButtonYes_Click"/>
                    </StackPanel>
                    <StackPanel Margin="8" Orientation="Horizontal" x:Name="ButtonYesNo" Visibility="Hidden">
                        <Button Style="{StaticResource btn}" Content="Yes" Margin="0,0,8,0" Click="ButtonYes_Click"/>
                        <Button Style="{StaticResource btn}" Content="No" Click="ButtonNo_Click"/>
                    </StackPanel>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</Window>
