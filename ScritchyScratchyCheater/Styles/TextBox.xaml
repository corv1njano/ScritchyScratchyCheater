<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="TextBox" x:Key="TextBox.Default">
        <Setter Property="Background" Value="{DynamicResource ControlElement.Flat.Background}"/>
        <Setter Property="FontFamily" Value="{StaticResource Montserrat.Medium}"/>
        <Setter Property="Foreground" Value="{StaticResource Font.Secondary}"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="Padding" Value="6,3"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource ControlElement.Flat.Border}"/>
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="TextBoxBorder"
                            Background="{TemplateBinding Background}" 
                            CornerRadius="4"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Focusable="false"
                                      VerticalAlignment="Center"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" TargetName="TextBoxBorder" Value="{StaticResource ControlElement.Flat.Border.Hover}"/>
                            <Setter Property="Foreground" Value="{StaticResource Font.Secondary.Hover}"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Background" TargetName="TextBoxBorder" Value="{StaticResource ControlElement.Flat.Background.Hover}"/>
                            <Setter Property="BorderBrush" TargetName="TextBoxBorder" Value="{StaticResource ControlElement.Flat.Border.Hover}"/>
                            <Setter Property="Foreground" Value="{StaticResource Font.Secondary.Hover}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>